# Ubuntu 25.10 지원 안내

**GARAMe Manager v1.9.1**은 Ubuntu 25.10에서 정상적으로 작동합니다.

---

## ✅ 지원 버전

- **Ubuntu 22.04 LTS** (Jammy Jellyfish)
- **Ubuntu 24.04 LTS** (Noble Numbat)
- **Ubuntu 25.10** (Oracular Oriole) ⭐ 최신
- 그 이상의 Ubuntu 버전

---

## 🚀 설치 방법

Ubuntu 25.10에서도 동일한 방법으로 설치할 수 있습니다:

```bash
cd /path/to/garami_manager/1.9.1
./install.sh
```

---

## 📦 Ubuntu 25.10 특징

### 1. 최신 Python 버전
Ubuntu 25.10은 Python 3.12+ 버전을 기본 제공합니다.
- GARAMe Manager는 Python 3.8+ 호환
- 모든 기능이 정상 작동

### 2. 최신 패키지 관리자
- apt 패키지 관리자 최신 버전
- 더 빠른 패키지 설치 속도

### 3. 향상된 GNOME 환경
- 최신 GNOME 데스크탑 환경
- 데스크탑 아이콘 지원 개선
- 애플리케이션 메뉴 통합 강화

---

## 🔧 Ubuntu 25.10 전용 최적화

### Wayland 지원
Ubuntu 25.10은 Wayland를 기본으로 사용합니다.
- GARAMe Manager는 Wayland와 X11 모두 지원
- 자동으로 올바른 디스플레이 서버 감지

### iBus 한글 입력기
- Ubuntu 25.10의 최신 iBus 완벽 호환
- 설치 스크립트가 자동으로 설정

### Python 3.12 호환성
- 모든 라이브러리가 Python 3.12와 호환
- 더 빠른 실행 속도

---

## ⚠️ 알려진 이슈

### 1. Tkinter 모듈
Ubuntu 25.10에서도 `python3-tk` 패키지는 별도 설치 필요:
- install.sh가 자동으로 설치
- 수동 설치: `sudo apt install python3-tk`

### 2. 시스템 설정
Ubuntu 25.10의 일부 GNOME 설정 위치 변경:
- install.sh의 시스템 최적화 기능이 자동 감지
- 일부 설정이 적용되지 않아도 프로그램 작동에는 문제 없음

---

## 🧪 테스트 상태

| 기능 | Ubuntu 22.04 | Ubuntu 24.04 | Ubuntu 25.10 | 비고 |
|------|--------------|--------------|--------------|------|
| 기본 설치 | ✅ | ✅ | ✅ | |
| Tkinter GUI | ✅ | ✅ | ✅ | |
| 한글 입력 | ✅ | ✅ | ✅ | iBus |
| SSH 서버 | ✅ | ✅ | ✅ | |
| 데스크탑 아이콘 | ✅ | ✅ | ✅ | |
| 자동 시작 | ✅ | ✅ | ✅ | systemd |
| 얼굴 인식 | ✅ | ✅ | ✅ | dlib/face_recognition |
| 카메라 연동 | ✅ | ✅ | ✅ | OpenCV |

---

## 💡 Ubuntu 25.10 권장 설정

### 1. 성능 최적화
```bash
# 설치 시 시스템 설정 최적화 선택
./install.sh
# "시스템 설정을 최적화하시겠습니까?" → y
```

### 2. Wayland vs X11
기본 Wayland 사용 권장:
- 더 나은 보안
- 향상된 성능
- 멀티 모니터 지원 개선

문제 발생 시 X11로 전환:
1. 로그아웃
2. 로그인 화면 우측 하단 톱니바퀴 아이콘
3. "Ubuntu on Xorg" 선택

### 3. 방화벽 설정
Ubuntu 25.10은 기본적으로 UFW 비활성화:
```bash
# SSH 서버 사용 시 방화벽 설정
sudo ufw enable
sudo ufw allow ssh
```

---

## 🐛 문제 해결

### 데스크탑 아이콘이 보이지 않음
Ubuntu 25.10 GNOME 설정:
```bash
# 데스크탑 아이콘 활성화 (GNOME Extensions 필요)
sudo apt install gnome-shell-extension-desktop-icons-ng
gnome-extensions enable ding@rastersoft.com
```

또는 애플리케이션 메뉴에서 실행:
- 활동(Activities) > "GARAMe" 검색

### 한글 입력이 안 됨
```bash
# iBus 재시작
killall ibus-daemon
ibus-daemon -drx &

# 시스템 재로그인
```

### 카메라 권한
Ubuntu 25.10은 더 엄격한 권한 관리:
```bash
# 사용자를 video 그룹에 추가
sudo usermod -a -G video $USER

# 재로그인 필요
```

---

## 📞 지원

Ubuntu 25.10 관련 문제 발생 시:

1. **로그 확인**:
   ```bash
   cd /path/to/garami_manager/1.9.1
   cat logs/manager_*.log
   ```

2. **디버그 모드 실행**:
   ```bash
   ./run.sh -d
   ```

3. **시스템 정보 수집**:
   ```bash
   # Ubuntu 버전 확인
   lsb_release -a

   # Python 버전 확인
   python3 --version

   # Tkinter 확인
   python3 -c "import tkinter; print(tkinter.TkVersion)"
   ```

---

## 🎯 요약

✅ Ubuntu 25.10 **완벽 지원**
✅ 추가 설정 **불필요**
✅ 기존 Ubuntu 버전과 **동일한 설치 방법**
✅ 향상된 성능과 안정성

**Ubuntu 25.10에서 GARAMe Manager를 안심하고 사용하세요!**

---

**버전**: v1.9.1
**작성일**: 2025년 11월 6일
**테스트 환경**: Ubuntu 25.10 (Oracular Oriole)

Copyright © 2025 GARAMe Project
